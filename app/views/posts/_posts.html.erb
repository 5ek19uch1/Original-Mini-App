<script>
  $(document).ready(function() {
    $('.posts').delay(1500).fadeIn(3000);
  });
</script>
<div class = posts>
  <% @posts.each do |post| %>
      <div class = contents>
        <h5><%= post.updated_at.strftime('%Y/%m/%d %H:%M')%></h5>
        <h3><%= post.content %></h3>
      </div>
      <div class = edit-menu>
        <P id = "edit-btn"><%= link_to '編集', edit_post_path(post) %></P>
        <P id = "edit-btn"><%= link_to '削除', post, method: :delete, data: { confirm: '消しちゃうの？' } %></P>
      </div>
  <% end %>
</div>